services:
  - type: web
    name: contactsphere-ml
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn contactsphere.run:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

  - type: postgresql
    name: contactsphere-db
    plan: free
